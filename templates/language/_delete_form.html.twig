<form method="post" action="{{ path('language_delete', {'id': language.id}) }}"
      onsubmit="return confirm('Etes vous sûr de vouloir supprimer ce language?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ language.id) }}">
    <button class="btn btn-danger"><i class="fas fa-trash-alt"></i> Supprimer</button>
</form>
